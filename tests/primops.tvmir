(define (fib n)
  (if (< n 2)
      n
      (+ (fib (- n 2))
         (fib (- n 1)))))

(define (fact n)
  (if (= n 0)
      1
      (* n (fact (- n 1)))))

(define (main args)
  (print (cons (typeof (car args))
               (fact (fib 4)))))
